# Django Settings
# WARNING: Change SECRET_KEY before deploying to production!
# Generate a new key with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-d!38_@gss&uf%ohlc6x_1+5hw%&qj*)ni@=g64_=e=a4)_#sjn
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# Set DATABASE_ENGINE to either:
#   - django.db.backends.postgresql (recommended for production)
#   - django.db.backends.sqlite3 (for development/testing only)
DATABASE_ENGINE=django.db.backends.postgresql

# PostgreSQL Configuration (used when DATABASE_ENGINE=django.db.backends.postgresql)
# For Docker: use DATABASE_HOST=db
# For local development: use DATABASE_HOST=localhost
# SECURITY: Change DATABASE_PASSWORD for production!
DATABASE_NAME=strategic_planner
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
